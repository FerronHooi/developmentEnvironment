o# Environment variables (contains secrets - NEVER commit these!)
.env
.env.local
.env.*.local

# Backup files
*.backup.*
*.bak
*.orig

# State capture files
state-snapshot.json
requirements.captured.txt
extensions.captured.json

# Custom local configurations
custom-setup.sh
local-settings.json
local-config.json
overrides.json

# Logs
*.log
logs/

# Temporary files
*.tmp
*.temp
.tmp/
.temp/

# IDE specific files (if not using VS Code settings sync)
.vscode-server/
.vscode-test/

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Python cache (if running scripts locally)
__pycache__/
*.pyc
*.pyo

# Node modules (if installed locally for testing)
node_modules/

# Docker volumes (if mounted locally)
volumes/

# Credentials and keys
*.pem
*.key
*.cert
*.crt
*.p12
*.pfx
credentials/
secrets/

# Ignore IDE specific folders (if not using VS Code settings sync)
.vscode/
.idea/
.devcontainer/